# .env.production — loaded by Vite when building with --mode production
# Override VITE_API_URL via docker-compose build arg for real deployments.

VITE_API_URL=https://api.example.com
VITE_USE_MOCK=false
VITE_PRICES_IN_CENTS=false
