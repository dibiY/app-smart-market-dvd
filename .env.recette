# .env.recette — loaded by Vite when building with --mode recette
# Override VITE_API_URL via docker-compose build arg for staging deployments.

VITE_API_URL=https://api.recette.example.com
VITE_USE_MOCK=false
VITE_PRICES_IN_CENTS=false
