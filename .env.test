# .env.test — loaded by Vite when building with --mode test
# All VITE_* vars are baked into the JS bundle at build time.

VITE_API_URL=http://localhost:8000
VITE_USE_MOCK=true
VITE_PRICES_IN_CENTS=false
